BAXSTAR CAPTION STUDIO — BUILD STATE (proxy architecture; decided 2026-06-28)
Frontend: GitHub Pages serving the landing at index.html + the tool at app/index.html (since 2026-07-06). Backend: Apps Script Code.gs proxy holding the key. Pattern mirrors baxstar-pontoon.

[x] 1. Repo set up: index.html (proxy frontend) + .gitignore + CLAUDE.md + this file, committed. — Done 2026-06-28: scaffolded at ~/Desktop/caption-studio; index.html = proxy file; dead-end pair → /reference; .claude/commands/caption.md added; first commit pushed to baxstarcode/caption-studio.
[x] 2. backend/Code.gs written: doPost; read token + Anthropic key from Script Properties; validate incoming {_token}; relay {messages} to https://api.anthropic.com/v1/messages with headers x-api-key, anthropic-version: 2023-06-01, content-type: application/json (NOT the browser-only dangerous-direct header); inject model "claude-sonnet-4-6" and max_tokens 1000 server-side; return Anthropic's raw JSON unchanged. Mirror baxstar-pontoon/Code.gs. — Done 2026-06-28: doPost validates {_token} against BAXSTAR_TOKEN + relays {messages} to claude-sonnet-4-6 with key from Script Properties (both secrets in Script Properties only); model + max_tokens injected server-side; raw Anthropic JSON passed through; added doGet sanity endpoint + setupCheck helper.
[x] 3. Local mock proxy written (mirror baxstar-pontoon's .devtest/mock_gas_server.py) so the frontend-to-proxy contract runs without the real key or a deploy. — Done 2026-06-28: .devtest/mock_gas_server.py mirrors the Code.gs contract (same validation order + exact error strings) and GAS CORS (POST → ACAO:*, OPTIONS preflight → 405 no-CORS); on a valid POST it synthesizes an Anthropic-shaped reply whose text block is the detection JSON the frontend parses, with test-only _mockDetection/_mockRaw overrides to drive any UI branch or the error path. 8-point curl check all green. Bench token caption-mock-token-2026 (NOT a secret); runtime log/report files gitignored.
[x] 4. Frontend hardened: posts ONLY {_token, messages} to PROXY_URL; handles data.error; sponsor chips toggle; hashtag count caps at 5; copy button works. Leave PROXY_URL and token as clearly-marked placeholders. — Done 2026-06-28: (a) CRITICAL — fetch now sends Content-Type: text/plain;charset=UTF-8 so the cross-origin POST is CORS-simple and triggers NO preflight (the application/json version would have died at an OPTIONS preflight GAS refuses); (b) data.error now surfaced via showError() with the real message (textContent, no HTML injection); (c) hashtags HARD-capped at 5 total counting #getyourbassingear (new capHashtags()) — output trims extras and the count reads "5/5 · N extra trimmed" instead of only warning; posts-only-{_token,messages}, chips, copy, and the PASTE_* placeholders left intact. VERIFIED in a headless Chrome run against the mock (served cross-origin on :8811 → proxy :8810): single POST→200, ZERO preflight (mock log shows 0 violations), full flow populated caption/bass/lake/chips, striker chip toggle removes its handle, 6 hashtags capped to 5. Browser-verified against the MOCK; live-URL verify is still step 7 (post-deploy).
[x] 5. DEPLOY.md runbook: create Apps Script project, paste Code.gs, set Script Properties (token + Anthropic key), deploy as web app (Execute as Brady, Access: Anyone), copy the /macros/s/.../exec URL, paste exec URL + token into index.html, enable Pages. Note: use the /macros/s/ exec URL, NOT a /macros/a/<domain>/ URL (login wall). — Done 2026-06-28: DEPLOY.md written as an ordered runbook (Part A Apps Script proxy → setupCheck authorize/confirm → web-app deploy with the /macros/s exec-URL + "Anyone" access trap called out hard; Part B wire the two index.html placeholders; Part C enable Pages; Part D live-verify = step 7) plus an "Updating later" new-version gotcha, an honest security-model section, and a troubleshooting table. Cross-checked against the code: property names (BAXSTAR_TOKEN/ANTHROPIC_API_KEY), model id, doGet service string, and the PASTE_* placeholders all match Code.gs + index.html; no real key in the doc.
[BRADY] 6. Provide an sk-ant- key; set Script Properties; deploy the web app; paste exec URL + token into index.html; enable Pages.
[x] 7. End-to-end verify on the live Pages URL with a real fishing photo: caption returns, sponsor chips toggle, hashtags cap at 5, copy works. Not done off a 200 alone. Record VERIFIED + live URL here.
       PARTIAL — verified 2026-06-28 (backend + deploy half): live page https://baxstarcode.github.io/caption-studio/ (HTTP 200, serving the wired index.html with exec URL + token, no sk-ant in source). Proxy doGet returns correct service/model JSON; a valid POST relays to claude-sonnet-4-6 and returns a real Anthropic reply (token validated, sk-ant key in Script Properties works, CORS header present); bad token rejected with {"error":"Bad or missing token"} BEFORE any Anthropic call (no credit burn); malformed body -> {"error":"Request body is not valid JSON"}.
       VERIFIED — 2026-07-02, live URL https://baxstarcode.github.io/caption-studio/ — full real-photo UI flow, driven in headless Google Chrome (Playwright, channel=chrome) against the LIVE Pages page (pre-checked byte-identical to repo index.html), real photo "Two boys and grandpa with nice crappies and walleye.webp". 19/19 assertions green: webp upload -> canvas downscale to jpeg thumb; ONE POST to the /macros/s exec URL, ZERO OPTIONS preflights; real vision round-trip 14.0s on claude-sonnet-4-6; detection JSON parsed and filled the UI — caption drafted on-voice, bass toggle correctly OFF (walleye+crappie photo), species-mapped hashtags #walleye #walleyefishing #crappie #crappiefishing (4/5), chips vexusBoat + sunglasses correctly ON (Vexus console visible frame-right; kid wearing shades), striker correctly conservative-OFF; chip toggle verified BOTH directions (on->off removes @therealvexusboats from preview, off->on adds @mercurymarine, and back); hashtag hard-cap verified both modes (7 entered, non-bass -> "5/5 hashtags · 2 extra trimmed"; bass ON -> #getyourbassingear takes a slot -> "5/5 · 3 extra trimmed"; boundary tags correct); copy button -> "Copied ✓" and clipboard readback BYTE-EQUAL to assemble().text (293 chars). Evidence (screenshots 01-06 + state.json with every assertion + network log): .devtest/live-verify-2026-07-02/ (gitignored, copied from the /tmp run dir). THE CAPTION TOOL IS SHIPPED.
[HOLD] 8. v2 merge plan only (do not build): preset/look catalog, social post builder, video-frame capture. Ship the caption tool first.
[x] 11. Sponsor set reconciled with Ember's Caption Studio; browser harness added. — Done 2026-08-01. THE DIVERGENCE: this tool and Ember's built-in Caption Studio had drifted into two different sponsor lists. Ember was tagging @vexusboats — the WRONG account; canonical is @therealvexusboats (Consolidation Notes §2), so every sponsored post published through Ember would have notified the wrong company. Fixed on the Ember side (baxstar-ember, branch claude/caption-studio-ember-pzm4xw). Coming back the other way, Ember carried two sponsors this tool never had, so Summit Fishing (@summitfishingequipment, with a detection key) and J&K Marine (@jandkmarine, manual — not something a model can see in a photo) were added here. Both surfaces now offer the same ten. NEW: .devtest/app-e2e.mjs drives app/index.html in headless Chromium with the proxy intercepted — no sk-ant- key, no billed call, no deploy. 16/16 green: zero CORS preflights, both photos in one read, posts only {_token, messages}, ten chips render, detection pre-arms Vexus + Summit while J&K and Power-Pole stay off, the 5-hashtag cap holds with the trim reported, and the clipboard text carries exactly 5 tags plus both new handles. Run: npm install --no-save playwright && node .devtest/app-e2e.mjs.
       WHY TWO SURFACES IS STILL RIGHT (not a bug to collapse): this tool is the bookmarkable one-tap URL at the lake, per the 2026-06-28 PROXY decision; Ember is the command center, and it owns the Drive-outbox publish path. Same rules, two front doors. The cost of that is exactly this kind of drift, so the sponsor list is the first thing to check whenever either side changes.
[x] 12. Caption body is now caption-only; handles + location moved to a composer checklist. — Done 2026-08-04, Brady's rules, THIS TOOL ONLY (he was explicit: "All of those changes should be made to the caption studio, they should not be included in Ember"). TWO RULES FOR #getyourbassingear: (a) bass posts only — it was already gated on the Bass switch and stays there; (b) when it appears it sits IMMEDIATELY above the business name with a BLANK LINE above it, so the tag/name/site block reads as one unit split off from the caption body. That blank line is the new part; assemble() now pushes "" before the brand block. THE BIGGER CHANGE: @handles and the lake no longer go into the copied text at all. A handle typed into a caption is plain text — it notifies nobody — and if you ALSO tag properly in Instagram's Tag People field (which is the whole point, and what the Zap's Tagged User(s) mapping does), every sponsor gets tagged twice. So assemble() returns { handles, lake } instead of appending them, and the Ready-to-post card carries a "Then in Instagram, before you share" checklist listing exactly what to type into Tag People and Location. Location is always a plain lake name or empty — NEVER an ID (Meta hard-rejected one on 2026-07-07). Facebook has the same two fields; same treatment. VERIFIED: .devtest/app-e2e.mjs now 23/23 green, including handles-in-checklist-not-preview, no "@" anywhere in the clipboard, bass tag directly above the business name with a blank line above it, the lake absent from the caption, and the tag vanishing when Bass is switched off. Screenshot of the finished card: .devtest/shot_ready.png (gitignored, regenerated by the harness).
[x] 9. Black/red landing-match restyle (2026-07-02, Brady's ask; source of truth = caption-studio-landing (3).html): index.html re-skinned to the landing's system — same CSS variables (ink #0a0a0b / red #c1272d>#e3262e / chromed silver), Barlow Condensed uppercase headers, red-gradient buttons, embedded chromed Baxstar logo, ambient bleed/topo/grain layers, landing footer. JS BYTE-IDENTICAL to the step-7-verified build (new head/body spliced onto the deployed file's own <script> block; char-count match proven). Regression vs a mock proxy: 10/10 green (upload, detection fill, chip toggle both ways, 5-cap, clipboard byte-equal, zero JS errors). 3-lens judge panel (palette/type, layout/spacing, readability): SHIP x3; all 7 findings fixed (condensed Bass-post label, in-brand disabled CTA, squared >=3:1 switch, squared thumb close, no resize grip, 7.4:1 trimmed-tags red). Deployed + live re-verified with a real caption (see chat 2026-07-02). NOTE: committed from a /tmp clone because the Desktop checkout was TCC-blocked mid-session — run `git pull` in ~/Desktop/caption-studio before local work.
[x] 10. Landing page shipped (2026-07-06, Brady picked root placement): caption-studio-landing (3).html installed as the Pages ROOT index.html with its two CTA hrefs relinked to relative app/; the tool moved UNCHANGED to app/index.html (git mv, content identical to step 9). New URLs: landing = https://baxstarcode.github.io/caption-studio/ (the shareable one), tool = https://baxstarcode.github.io/caption-studio/app/ (Brady must re-bookmark). Pre-push local checks 7/7 (root=landing, logo, CTAs->app/, tool loads at app/, JS init, upload enables analyze, proxy config intact); live-smoked after deploy WITHOUT an API call. Also committed from the /tmp clone (Desktop still TCC-blocked).

OPEN QUESTIONS FOR BRADY: (none — all decided)
- [DECIDED 2026-07-02, Brady in chat: "ship as is"] PROXY_TOKEN ships inside the public Pages index.html, so anyone who views source can read it and call your proxy. It's a soft gate, not a secret; the real protection is the bounded server-side call (one model, max_tokens 1000) capping abuse cost — same accepted tradeoff as pontoon. Decision: SHIP AS-IS. No Code.gs abuse mitigation now; revisit only if real abuse shows up in Apps Script executions/API usage.
[x] 13. PWA installability — the tool installs like an app (loop iteration 1, 2026-08-07). Brady asked for Caption Studio "to the level of a mobile app"; the floor for that is installing like one. New: app/manifest.webmanifest (standalone display, portrait, ink theme), app/sw.js (stale-while-revalidate shell cache mirroring baxstar-ember/sw.js — same-origin GETs only, so the proxy POST is never cached and captions are never stale; offline navigations fall back to the cached shell), app/icons/ (192/512/maskable/apple-touch, rendered from the tool's own embedded chromed logo onto the ink ground with the landing's red under-glow), and head metadata (theme-color, apple-mobile-web-app-*, manifest + apple-touch-icon links) plus a guarded registration snippet that is a silent no-op anywhere a worker can't run. On iPhone: Share → Add to Home Screen → full-screen app named "Captions" with instant repeat opens on lake signal. VERIFIED: .devtest/app-e2e.mjs extended with two PWA assertions (manifest linked/served/standalone with 3 icons; sw.js + icons served, apple-touch + theme-color present) — 25/25 green, zero page errors, the full caption flow untouched. NEXT LOOP CANDIDATES (one per iteration): Web Share Level 2 (share caption + photo straight into the Instagram app — kills the copy-then-switch dance), camera capture attribute + safe-area insets, offline caption queue.
[x] 14. Outbound share — photo + caption into the native share sheet (loop iteration 2, 2026-08-14). The other half of entry-13/PR-4's inbound share: a "Share photo + caption" button on the Ready-to-post card. One tap copies the caption to the clipboard FIRST (iOS hands Instagram the image but silently drops share text — the clipboard is the plan, and the confirmation label says so), then opens navigator.share with the photos as real Files at FULL quality: the intake now retains the original File alongside the 1280px model render (both picker and shared-in paths; a reconstruction fallback from b64 covers entries that predate this). No share sheet (desktop) → the button degrades to a plain copy and says so; closing the sheet is treated as a cancel, not an error. VERIFIED: harness stubs the share sheet — 4 new assertions (2 photos arrive as real Files with text; clipboard holds the @-free caption before the sheet opens; every intake entry is shareable; the label points at the clipboard) — 29/29 green, zero page errors. NEXT LOOP CANDIDATES: camera capture + safe-area insets, offline caption queue, install prompt nudge.
[x] 15. Camera capture + app-feel touch (loop iteration 3, 2026-08-15). A "Take a photo now" button under the drop zone wired to a hidden input with capture="environment" — on a phone it opens the REAR CAMERA directly, no picker: fish still in hand, photo straight into the flow. All three arrival paths (picker, camera, share sheet) now run through one shared intake(), so the full-quality original File is retained uniformly for the outbound share. Touch polish: -webkit-tap-highlight-color transparent + touch-action:manipulation on controls (kills the gray tap flash and the double-tap-zoom delay), overscroll-behavior-y none (no rubber-band in the installed app). Safe-area body padding already existed and is now locked by an assertion. VERIFIED: 3 new harness checks (capture attrs; a camera shot lands via the same intake with its original File and renders a thumb; computed touch-action + safe-area rule present) — 32/32 green, zero page errors. NEXT LOOP CANDIDATES: offline caption queue, install nudge, haptic-weight microinteractions.
